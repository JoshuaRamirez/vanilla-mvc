**Why** — the problem, or the issue this closes

**What changed**

- [ ] `npm test` passes
- [ ] a test that fails without this change
- [ ] a rule change has an example in `test/architecture-examples.test.mjs` and `npm run rules:md` was run
- [ ] CHANGELOG.md, under *Unreleased*, if users would notice
